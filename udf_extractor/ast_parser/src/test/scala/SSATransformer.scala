package ast_parser

import org.scalatest._
import Matchers._
import scala.meta._

class SSATransSpec extends FunSuite with Matchers {
    test("naive") {
        // assert(q"a+b+c" == q"a+b+c")
    }
}
